#!/usr/bin/node
/*
script that prints two arguments passed to it
*/

multi_str = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let i = 0; i < multi_str.length; i++) {
  console.log(multi_str[i]);
}
